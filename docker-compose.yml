services:
  backend:
    build: .
    ports:
      - 5001:5001
      - 9229:9229
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run start:docker
    environment:
      PORT: 5001